# =============================================================================
# Wedding Memories — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values.
#
#   cp .env.example .env
#   pnpm dev
#
# =============================================================================

# -----------------------------------------------------------------------------
# App configuration (NEXT_PUBLIC_* = available in both server and client code)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_BRIDE_NAME=Bride
NEXT_PUBLIC_GROOM_NAME=Groom
NEXT_PUBLIC_GUEST_ISOLATION=false
# Supported values: en (English) | ms (Malay)
NEXT_PUBLIC_DEFAULT_LANGUAGE=en
# WhatsApp number for support (international format without +, e.g., 60123456789)
NEXT_PUBLIC_WHATSAPP_NUMBER=

# -----------------------------------------------------------------------------
# Server-only security configuration
# -----------------------------------------------------------------------------
# Gate access to gallery + uploads. Leave empty to disable token requirement.
# Recommended: 48-64 random chars (e.g., `openssl rand -base64 48`)
EVENT_TOKEN=

# Rate limiting (server-side). Values are per guest when EVENT_TOKEN is set.
# Upload limit defaults to 60 uploads / 5 minutes.
RATE_LIMIT_UPLOAD_WINDOW_MS=300000
RATE_LIMIT_UPLOAD_MAX=60

# Gallery list limit defaults to 60 requests / 1 minute.
RATE_LIMIT_PHOTOS_WINDOW_MS=60000
RATE_LIMIT_PHOTOS_MAX=60
