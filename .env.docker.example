# =============================================================================
# Wedding Memories — Docker Environment Variables
# =============================================================================
# Copy this file to .env (next to docker-compose.yml) and customise.
#
#   cp .env.docker.example .env
#   docker compose up -d --build
#
# =============================================================================

# -----------------------------------------------------------------------------
# App configuration (NEXT_PUBLIC_* = available in both server and client code)
# These are baked into the Docker image at build time.
# Changing them requires rebuilding: docker compose up -d --build
# -----------------------------------------------------------------------------
NEXT_PUBLIC_BRIDE_NAME=Bride
NEXT_PUBLIC_GROOM_NAME=Groom
NEXT_PUBLIC_GUEST_ISOLATION=true
# Supported values: en (English) | ms (Malay)
NEXT_PUBLIC_DEFAULT_LANGUAGE=en
# WhatsApp number for support (international format, e.g., 60123456789)
NEXT_PUBLIC_WHATSAPP_NUMBER=
# PORT=3010
