# =============================================================================
# Wedding Memories — Docker Environment Variables
# =============================================================================
# Copy this file to .env (next to docker-compose.yml) and customise.
#
#   cp .env.docker.example .env
#   docker compose up -d
#
# =============================================================================

# -----------------------------------------------------------------------------
# Storage provider: local | cloudinary | s3
# -----------------------------------------------------------------------------
# "local"      — saves to a Docker volume, no cloud credentials needed
# "cloudinary"  — uses Cloudinary (fill in CLOUDINARY_* below)
# "s3"          — uses S3/Wasabi  (fill in AWS_* / S3_* below)
NEXT_PUBLIC_STORAGE_PROVIDER=local

# -----------------------------------------------------------------------------
# App configuration
# -----------------------------------------------------------------------------
BRIDE_NAME=Bride
GROOM_NAME=Groom
GUEST_ISOLATION=false
# Supported values: en (English) | tr (Turkish) | ms (Malay)
DEFAULT_LANGUAGE=en
# PORT=3010

# -----------------------------------------------------------------------------
# Cloudinary (only needed when NEXT_PUBLIC_STORAGE_PROVIDER=cloudinary)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret
# CLOUDINARY_FOLDER=wedding

# -----------------------------------------------------------------------------
# S3 / Wasabi (only needed when NEXT_PUBLIC_STORAGE_PROVIDER=s3)
# -----------------------------------------------------------------------------
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# NEXT_PUBLIC_S3_BUCKET=your-bucket
# NEXT_PUBLIC_S3_ENDPOINT=https://s3.us-east-1.wasabisys.com
